services:
  nginx:
    image: nginx:alpine
    container_name: nginx-odoo
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/odoo.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
 

